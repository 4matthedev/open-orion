# Open Orion — full dependency set.
#
# These are mirrored in pyproject.toml under [project.optional-dependencies]
# as `api`, `voice`, `voice-pipeline`, and `all`. New installs are encouraged
# to use the package instead:
#
#     pip install -e ".[all]"          # everything below
#     pip install -e ".[voice]"        # voice without the legacy pipeline
#
# The CLI core (no cloud API / no voice) only needs the first four lines.
pydantic>=2.7,<3
pydantic-settings>=2.3,<3
httpx>=0.27,<1
rich>=13.7,<14
litellm>=1.40
faster-whisper>=1.0
kokoro-onnx>=0.4
espeakng-loader>=0.2
piper-tts>=1.3
coqui-tts>=0.24
f5-tts>=1.1
transformers>=4.57,<5
